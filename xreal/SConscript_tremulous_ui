import os, string, sys

Import('env')

# the file with vmMain function MUST be the first one of the list
ui_src = [
'tremulous/code/ui/ui_main.c',
'tremulous/code/ui/ui_atoms.c',
'tremulous/code/ui/ui_gameinfo.c',
'tremulous/code/ui/ui_players.c',
'tremulous/code/ui/ui_shared.c',
'tremulous/code/game/bg_misc.c',
'code/qcommon/q_math.c',
'code/qcommon/q_shared.c'
]

dll_src = [
'tremulous/code/ui/ui_syscalls.c'
]

ui_env = env.Clone()

if env['arch'] == 'linux-i386':
	ui_env.SharedLibrary('ui', [ui_src, dll_src], LIBS=['m'])
	ui_env.InstallAs('#tremulous/uii386.so', 'libui.so')

elif env['arch'] == 'linux-x86_64':
	ui_env.SharedLibrary('ui', [ui_src, dll_src], LIBS=['m'])
	ui_env.InstallAs('#tremulous/uix86_64.so', 'libui.so')





